import { colorType, sizesType, iconType } from "../../types/types";
interface Props {
    color?: colorType;
    size?: sizesType;
    icon?: iconType;
}
export declare const SButton: import("styled-components").StyledComponent<"button", any, Props, never>;
export {};
